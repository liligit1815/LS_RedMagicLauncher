# 开发检查点：260015

本文件保存截至 2026-09-26 的项目状态，使新电脑只依靠本仓库及授权的私有签名备份即可接续开发。当前包名为 `com.zte.mifavor.launcher`，源码内部版本 `260015`，显示版本 `26.9.260.908.2609081608`。用户此前对 `260005` 反馈“测试通过没有问题”，未逐项列出测试用例；该反馈不代表后续版本已获得完整验收。最新要求为桌面进入最近任务有清晰过渡；长按参考录屏的卡片与按钮展开动画，同时保留原厂右上角操作按钮；“更多”菜单完整关闭或操作完成后才恢复默认图标、名称规则。

用户随后实测反馈虚化图标被切半、入场大小回摆、间距未跟随缩放及过渡不自然。当前同轮修正将裁剪从整个任务视图移到截图子视图，图标保留完整轮廓而手势仍使用逻辑截图边界；卡片中心偏移与尺寸同比例缩放，实时入场统一从松手大小插值到用户目标，后方截图跟随同一进度淡入。新增整段入场、间距及实际 Smali 裁剪分支检查，并扩大删除补位和截图保留回归；实际视觉效果与帧率尚待装机验收。

260011 最后一次录屏反馈拖动前方卡片时后方操作按钮异常露出。该轮曾将操作区遮挡边界与截图裁剪边界分开，要求整个操作区完整容纳（现已被 260012 的第一张卡片规则替代），并在绘制时阻止已隐藏按钮因原厂 alpha 动画而重新出现。名称仍按自身空间独立显示。723 项生命周期检查和 78 条实际 Smali 绘制分支检查通过，实机效果待复测。

## 260015 桌面入场与长按操作过渡

当前以用户最终明确的两种入口为准：桌面进入顶层卡居中、后卡向左展开；App 进入前卡在右、第二卡居中、后卡向左展开。桌面后卡从顶层卡下方连续移动至各自原有堆叠落点，前卡不横移，任务顺序、用户缩放与稳态布局保留。单任务保持居中。桌面展开仍使用同一个 360 ms 过渡，App 入口继续使用原实时手势动画。

所有卡片默认隐藏操作按钮，长按仅显示所选卡片的原厂右上角按钮。包括第一张和单任务在内均无默认显示例外。长按松手及打开更多保持状态，菜单实际移除、操作完成或取消后全部隐藏。隐藏按钮的触摸区域另按几何阻断，防止 alpha 为 0 或原厂 alpha 写回时仍触发操作。原厂按钮布局、菜单内容和执行功能保持。

本次新增 795,600 项顶层居中/后卡左展几何检查、75 项按钮命中检查，生命周期回归 89,110 项；源码检查已恢复并保留桌面/App 两种终点的区分。此前尝试的“桌面第二卡居中”已撤回，构建中止，未交付。此前 08:00 和 08:23 的 260015 包均不含本次最终规则；历史构建记录、逐帧依据和验证范围见 [RECENTS_ENTRY_AND_ACTION_TRANSITIONS.md](RECENTS_ENTRY_AND_ACTION_TRANSITIONS.md)。

本次最终规则完整构建为 `signed_verified`，实际 APK 版本 `260015` / `26.9.260.908.2609081608`。全部源码回归、辅助 Java/Smali 同步、强制回编译、APK 实际版本、原签名身份、资源/原生库/assets 保留、DEX 类边界和 16 KB 对齐核验通过，构建期间源码未变。产物为 `out/build-260015-20260926-085922-542401/LS_红魔桌面修改版-26.9.260.908.2609081608-260015.apk`，大小 31,346,993 字节，SHA-256 为 `f527867daab093d9fb8e650b444d540b782b9d079a7e2af9012bcd9a45953f35`。本次包替代此前 08:00/08:23 的 260015 包供复测；未安装或回读手机版本，实机效果尚待验收。

## 260014 名称遮挡与快捷长按操作

上下拖动时截图需要解除裁剪，旧实现同时把拖动卡片移出名称的遮挡边界，导致后方名称误判为能完整显示。现在名称与图标使用独立于截图的遮挡边界，保留仍在任务列表中的前方拖动卡片；上下拖动不再因此释放名称空间，真正移除卡片后按新布局重新判断。

长按期间仅所按卡片显示原位置操作按钮，其上层卡片的图标、名称和按钮全部隐藏；绘制时额外拦截上层图标和名称，防止原厂透明度动画把它们重新画出。长按不自动打开菜单。操作完成、点击外部、无效操作、触摸取消、任务身份改变或退出最近任务后清除临时状态，恢复仅第一张卡片按钮显示，图标和名称恢复各自空间规则。

堆叠卡片长按阈值设为 250 ms（系统阈值更短时取更短值），不修改系统全局设置。超过触摸滑动阈值、多指、取消及视图解绑使排队回调失效。触发后取消原触摸流，避免松手误打开应用；首次松手仍保留按钮，供后续点击。非堆叠样式继续使用原厂行为。新增 CardLongPress 辅助内部类，当前新增类精确边界为九个，旧版本按其原有数量验证。

主机生命周期回归 16,855 项检查通过，覆盖快捷长按、拖动及过期回调、首次松手、无效操作恢复、上层标题隐藏和上下拖动时名称遮挡。另有 637,859 项入场/缩放连续性与 96 条实际 Smali 绘制路径检查通过。这些检查不等同于实机触摸或 GPU 绘制验收。

完整构建为 `signed_verified`，源码回归、辅助代码同步、强制回编译、APK 实际版本、原签名、资源/原生库保留、DEX 边界与 16 KB ZIP 对齐检查全部通过。产物位于 `out/build-260014-20260926-004409-153219/LS_红魔桌面修改版-26.9.260.908.2609081608-260014.apk`，大小 31,346,993 字节，SHA-256 为 `f237518e9b7777b2f0a3e36b93241bf26686440aa78ce986fc7073ba627c3ea6`。ADB 无连接设备，未安装或回读手机版本；需实机复测上下拖动时名称、长按后上层标题隐藏、首次松手保留按钮、有效/无效操作恢复以及快速滑动不误触长按。

## 260013 桌面上滑堆叠入场动画

用户录屏 `123.mp4` 首次进入约 0.85 秒、第二次约 4.40 秒时，卡片已经以接近最终尺寸出现；首轮还可见短暂黑色截图。代码确认：桌面预览父层缩放/位移被归一为终点值，同时 Overview 状态入口无条件结束而不启动堆叠 reveal，导致没有承担桌面卡片入场的动画。

现在仅从桌面进入时，在首轮合成前将卡片设为透明起点，等待有效任务顺序与布局后运行 280 ms 上移/淡入。上移距离为次轴尺寸的 8%，不改变自定义尺寸、横向位置或堆叠间距；应用入口仍使用原实时动画，避免两套动画叠加。取消、离开、手势接管和重进按动画代次隔离，旧回调不能清除新动画；加载超过首次重试窗口时由后续加载/绘制启动，不能跳过过渡直接显示终点。

生命周期回归执行实际动画监听器及合成方法，16,784 项检查通过，覆盖 51 档缩放、100 个进度采样、首帧隐藏、单调上移/淡入、恒定尺寸/横向位置、重复通知、延迟加载、快速重进、旧回调、取消及应用入口隔离。相同新增用例在修改前源码上失败于“Home entry was not armed before composition”。主机替身测试不等于真实 GPU 或实机动画验收，也不能保证慢截图首帧黑色问题已消除。

完整构建为 `signed_verified`，全部源码回归、辅助代码同步、强制回编译、实际 APK 版本、原签名、资源与原生库保留、DEX 边界和 16 KB 对齐核验通过。产物位于 `out/build-260013-20260926-002835-591868/LS_红魔桌面修改版-26.9.260.908.2609081608-260013.apk`，大小 31,346,993 字节，SHA-256 为 `fd13a2cebc409bf2300a3c103a581f70789be0938815841342f437f5585a6e7a`。未连接手机，实机上滑、快速取消重进、横屏与自定义比例仍待复测。

## 260012 第一张卡片按钮与长按临时显示

用户最终明确：默认第一张卡片显示操作按钮，其它卡片隐藏；长按只显示该卡片原位置按钮，不自动打开菜单，操作完成或取消后恢复默认。此规则替代 260011 的操作区空间判断，不随居中分页切换。名称和图标仍独立处理。

临时显示以任务视图及任务 ID 双重绑定，卡片本体、应用图标和菜单图标的长按均走显示按钮路径；点击按钮后才执行原厂功能。显示的按钮脱离截图切片裁剪，重叠区域点击直接送达原按钮，隐藏按钮拦截命中。长按松手不会立即取消；后续按钮操作、取消触摸、点击外部、原菜单关闭和退出最近任务清除临时状态。非堆叠样式保留原厂行为。

1013 项主机生命周期检查、78 条实际 Smali 绘制分支及既有入场/缩放连续性检查通过。完整构建为 `signed_verified`，版本、原签名、16 KB 对齐、资源/原生库保留、DEX 边界与辅助源码同步核验通过。产物位于 `out/build-260012-20260926-001250-674302/LS_红魔桌面修改版-26.9.260.908.2609081608-260012.apk`，大小 31,346,993 字节，SHA-256 为 `230520f92158d52c5f990825c7d5ec840977ee7b53d1484256a5dd93552fa453`。当前没有连接手机，实机交互与安装后版本尚待验收。

## 260011 图标空间显示与堆叠卡片缩放

图标按自身空间平滑淡入淡出及轻度虚化，去掉标题更新造成的每帧隐藏/显示切换；名称按实际文字边界独立显示，操作区另行判断。70%–120% 冰蓝拖动条嵌入堆叠样式卡片，共享底色及内边距，数值自动保存。桌面预览入口去掉整组任务额外 1.5 倍放大和左侧待机偏移，并保护小于 100% 设置的应用入场交接。2,530 项控件、缩放、虚化与显示范围断言及 561 项手势生命周期断言通过；实际帧率、绘制与安装后版本尚待实机验收。实现、构建与验证范围见 [STACK_SCALE_AND_ICONS.md](STACK_SCALE_AND_ICONS.md)。

## 260010 入场抖动与动画连续性

重复的 Overview 通知不再重开入场事务，加载回调保留有效任务顺序；每帧计算前校正原厂直接写入造成的坐标覆盖，桌面入口在任务身份未就绪时隐藏缓存首卡。实时应用画面的滚动、中心和深度缩放修正随原厂入场进度逐步生效；快速切换期间保留原厂目标缩放，长按手势入场仍持续重绘第二张卡片的移动。

用户继续反馈左右滑动闪屏、切后台左右抖动。同轮追加修正原厂分页范围会卸载堆叠可见截图的问题，并将实时入场的实际分页跳转延后到动画交接或用户接管。新增 13,689 项截图保留与分页连续性断言，手势生命周期扩展到 561 项，另有 624 项实时画面坐标断言；新增提前跳页用例在修正前源码上失败。当前 ADB 无连接设备，实际流畅度、安装后版本及各手势组合仍待实机验收，构建结果与详细范围见 [RECENTS_ANIMATION_CONTINUITY_FIX.md](RECENTS_ANIMATION_CONTINUITY_FIX.md)。

## 260009 小窗取消残留与手势条切换

上一版只限制了实时应用画面的变换，截图卡片仍可能在原厂手势期间被堆叠逻辑重排、裁剪和定页。本轮在原厂应用手势初始化之前清除缓存卡片的堆叠变换及裁剪，使待入场回调失效；手势拖动、取消、快速切换和随后的延迟回调均保留原厂分页与截图处理。明确选择 `RECENTS` 后才重新启用堆叠；从桌面再次进入最近任务也会恢复。

新增执行真实生命周期、整段合成方法和队列回调的 149 项 JVM 断言，上一版在拖动期间截图保真检查失败，新版通过。原有旋转、小窗实时变换、卡片命中等回归继续通过。完整签名构建为 `signed_verified`，真实 APK 版本、原开发签名、资源保真与代码边界核验通过。手机未连接，实际取消小窗、手势条切换以及正常最近任务入场仍待用户复测，详见 [NATIVE_GESTURE_OWNERSHIP_FIX.md](NATIVE_GESTURE_OWNERSHIP_FIX.md)。

## 260008 应用上滑拖入小窗动画修复

录屏中应用进入右上角小窗触发区域后，画面仍停留在居中的大卡片位置，松手后才缩到角落。对照原厂底包发现，原厂手势和小窗代码仍在，但辅助层把“最近任务视图待入场”误作“手势已提交到最近任务”，在拖动期间提前清零横向滚动、限制缩放并重置画面中心。

现将三处实时变换修正限定为明确的 `RECENTS` 结束目标；尚未松手或结束目标为小窗/桌面/应用时保留原厂输入。Java 与打包 Smali 已同步，实时画面回归从 434 项扩展到 670 项，覆盖原有旋转坐标及小窗方向的缩放、位移、拖回、目标切换和样式隔离。旧实现对拖动期间矩阵保真检查失败，新实现通过。完整签名构建为 `signed_verified`，实际 APK 版本、原签名、资源与代码边界核验通过。本轮未连接手机，尚未完成实机验收，详见 [MINI_WINDOW_GESTURE_FIX.md](MINI_WINDOW_GESTURE_FIX.md)。

## 260007 右侧卡片上滑命中修复

用户录屏显示上滑右侧可见卡片时，中间卡片被带动。原命中逻辑优先选择中间焦点卡，且忽略其裁剪范围，导致被右侧卡覆盖的区域仍能选中中间卡。现统一检查所有卡片的可见裁剪范围，按实际堆叠 Z 层级选择触点处最上层卡片，再交给原厂手势控制器。Java 和打包 Smali 已同步。

新增真实 Java 方法的 53 项触摸回归及控制器接入检查，已加入统一构建；原代码在右侧重叠触点用例失败，修复后通过。完整签名构建为 `signed_verified`，实际 APK 版本、原签名、资源保真与代码边界核验通过。本轮未连接手机，尚未安装或完成实机手势验证。详细实现及验收范围见 [RECENTS_TOUCH_TARGET_FIX.md](RECENTS_TOUCH_TARGET_FIX.md)。

## 260006 横屏入场修复

修正实时应用 surface 的目标中心计算：使用最近任务的实际布局和方向轴，再通过 TaskViewSimulator 同一帧的窗口旋转/偏移矩阵映射到 surface 坐标。此前直接使用竖屏 Resources 的 X/Y 尺寸，在横屏窗口中造成明显下移，实时动画交接后才恢复。Java 与打包 Smali 已同步，新增 434 项坐标断言并接入统一构建。详细根因、实机结果与未覆盖范围见 [LANDSCAPE_RECENTS_FIX.md](LANDSCAPE_RECENTS_FIX.md)。

## 当前功能边界

本修改版保留原厂桌面基础，增加最近任务堆叠样式、样式入口、进入/退出动画、可见卡片点击、上滑删除及补位适配。应用内进入多任务时选择第二张任务卡，从桌面进入或仅一个任务时选择第一张。桌面项目与 LS_Augment 模块独立构建、签名和管理版本。

260004 已移除自定义内存显示、整页排序、空白页管理及最近任务额外覆盖层，详见 [PAGE_FEATURES.md](../PAGE_FEATURES.md)。260005 没有恢复这些功能；不要误以为模块已经接管了所有移除项。

## 260005 已完成的实际修复

| 问题 | 当前实现与验证 |
|---|---|
| STACK-01 删卡补位终点不一致 | 统一删卡与最终布局的位置/缩放计算，保留分数视口；处理取消、重复回调、非焦点卡、2张变1张和下拉锁定状态。真实辅助 Java 的几何/状态回归通过。 |
| STACK-02 堆叠触摸规则影响原厂样式 | 非堆叠样式在入口返回原触摸事件，不修改堆叠分页状态；样式隔离检查通过。 |
| STACK-03 焦点提示被跳过 | 将跳过回弹的分支移到焦点绘制之后；原厂焦点辅助的 Smali 绘制路径检查通过。 |
| STACK-04 旧入场回调抢回页面 | 用户开始操作时使排队中的旧入场回调失效；确定性队列执行真实方法验证通过。 |
| BUILD-001 后续构建可能丢失对齐 | 强制回编译，签名前后进行 16 KB ZIP 对齐检查，核验真实版本、证书和包内内容。 |

主要实现位于 `helper-src/main/com/android/quickstep/views/LsNativeStack.java`；原厂接入位于 `src/smali_classes2/com/android/quickstep/views/RecentsView.smali` 和 `RecentsView$23.smali`。辅助 Java 修改后需同步对应 Smali，不能只改 Java 就跳过同步门禁。

## 已完成的工程独立与换机修复

- 工程自带 `original.apk`、Apktool、实际框架缓存和当前完整 Apktool 源码，不依赖模块目录。
- 固定回归基线改为跟踪的 `baselines/launcher-260005.apk`，不再依赖旧电脑被忽略的 `out/`。默认基线缺失或哈希不符时，在调用工具和读取签名配置前拒绝构建。
- `.gitattributes` 固定源码换行、保持打包资源字节原样。真实 Windows 干净克隆曾因辅助 Smali 的 LF/CRLF 差异被误拒绝；现只允许对应辅助主类/内部类的精确换行等价，其他字节和文件差异仍拒绝。
- 已用全新的实际 Git 克隆，在无 `.signing/`、无旧输出、不指定 `--previous` 的情况下完成未签名构建。全部 9,022 个非签名 APK 成员与验收基线一致，含所有 DEX、Manifest、资源、原生库及 assets。
- 固定基线门禁的 14 项检查、换行边界的 15 项检查通过。归档的详细设备/构建日志未随公开仓库上传；本文件和可执行源码检查共同保存继续开发所需的结论。
- 已将源码和原签名分别上传至公开/私有 GitHub 仓库，随后全新克隆两个仓库，实际恢复原密钥并完成签名构建；全部 9,022 个非签名成员与验收版一致，源码工作区保持干净。详见 [GitHub 恢复构建验收](REMOTE_RECOVERY_VERIFICATION.md)。

## 可重复的验收输入

| 输入 | SHA-256 |
|---|---|
| `original.apk` | `cce32bcfa1fce5a4c208ea510594a1c7d0e6ad614588334a12753f2819cf3f7c` |
| `baselines/launcher-260005.apk` | `2b5ccc21b361b910214df27930370b246b0495d8997e79e457241584dfe365df` |
| 原修改版签名证书 | `4e5c23c41de3d7d56f120309e9fe8dab7d53483c50ef18a176e00558abf7b8cb` |

原厂底包为 `160000 / 16.0.010.000.2604151532`。原厂证书与修改版证书不同；公有仓库中的签名证书和已签 APK 不能还原私钥。制作能延续当前安装身份的签名包，需要从私有 `LS_SSHKey` 仓库恢复本项目签名材料。

## 仍未完成或不能外推的事项

1. 安装前自动兼容性检查/拦截工具尚未实现。
2. 已保存设备数据及旧 APK 的维护者备份，但尚未演练完整恢复，不能声称一键回滚已经可靠可用。
3. 实机证据仅覆盖 NX809J、Android 16、RedMagicOS11.5.7MR1、4 KB 页大小的既有修改版升级。其他机型、固件、首次替换原厂桌面与 16 KB 运行环境仍需验证。
4. 用户整体反馈通过不等于横竖屏、分屏、重启、无障碍、所有手势与极端任务状态都有逐项覆盖记录。扩大支持范围前应补齐对应实测。
5. `260006` 针对横屏实时画面坐标问题，`260007` 针对右侧可见卡片上滑命中错误，`260008` 针对原厂小窗拖动变换被覆盖；历史审查中异常回退、无障碍遮挡等其他尚无充分证据的候选，不应一并写成已经证实或修复的缺陷。

兼容性细节见 [INSTALL_COMPATIBILITY.md](INSTALL_COMPATIBILITY.md)。保持原开发签名不会获得原厂签名权限，也不能用“Root 已开启”代替兼容性验证。

## 新电脑接续开发

按照 [README.md](../README.md) 克隆工程、安装已验证工具链并执行构建；需要签名时使用 `tools/restore-signing.py` 从授权的私有仓库恢复。本轮修改使用 `260015`，下一轮独立功能修改使用 `260016`，显示版本由用户决定；纯说明、备份或构建工具修正不另改 APK 版本。

后续工作根据用户的新需求确定范围，不自动扩展到安装预检、恢复演练或其他设备适配。修改后继续执行现有源码、包内边界、资源保真和签名验证，再进行针对实际改动的设备验收。
