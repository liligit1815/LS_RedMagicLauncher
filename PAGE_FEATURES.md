# 260004 功能拆分历史：页面管理与内存显示

本文记录 `260004` 时移除页面管理与内存显示的范围。当前源码已是 `260005`，显示版本保持 `26.9.260.908.2609081608`；后续修复未恢复本文列出的已移除功能。`260005` 已安装，用户反馈“测试通过没有问题”，未逐项列出测试用例。项目与构建方法见 [README.md](README.md)，可继续开发的完整检查点见 [DEVELOPMENT_STATUS.md](docs/DEVELOPMENT_STATUS.md)。

本版桌面仅负责最近任务堆叠样式，包括样式选择入口、卡片排列、进入和退出时的动画适配、可见卡片点击、上滑删除及补位。应用内多任务进入时聚焦第二张卡，桌面进入或只有一个任务时聚焦第一张卡。

## 本轮移除的增强

| 增强功能 | 本版处理 |
|---|---|
| 最近任务基础内存显示 | 移除设置开关、显示区域、内存读取与定时刷新 |
| 自定义内存显示 | 移除数值格式、百分比、颜色、字号、位置、横竖屏适配及模块设置读取 |
| 整页排序 | 移除整页拖动、边缘翻页、前后移动菜单及自定义页序恢复 |
| 空白页管理 | 移除新增和删除空白页菜单、保留空页、页 ID 去重及自定义删除记录恢复 |
| 最近任务额外界面调整 | 移除自定义空任务提示覆盖层、清理按钮位移与相关状态维护，交由原厂界面处理 |

相关执行代码、辅助类、调用入口和专用资源随功能一起移除。LS_Augment 的独立实现属于后续阶段，本轮没有完成这些功能向模块的迁移。即使现有模块里仍保留相关设置，本版桌面也不会再读取这些设置来提供上述增强。

## 已有页面记录与使用效果

旧版本将页序、正式空白页和明确删除页保存在桌面的私有设置中。本轮不添加清空旧记录或重写图标、文件夹、组件位置的操作；本版也不再读取、恢复或更新这些自定义记录。

在 LS_Augment 尚未接管时，桌面绑定、页面排序、空页清理和新页 ID 分配恢复原厂处理。已有正式空白页可能被原厂清理，自定义页序可能按原厂规则重新呈现，原先明确删除的空页也不再享有自定义恢复保护。旧记录即使仍存在，也不能替代已移除的执行代码。

后续模块迁移需要适配旧记录，并验证迁移期间发生页面变化时的恢复结果。不能把本轮拆分理解为增强功能已由模块继续提供。

## 验证范围

本版保留堆叠入口选择、任务列表更新及堆叠行为检查，并增加已移除功能不得残留在 Java、Smali 或资源中的静态检查。此前页面排序、空页持久化和内存显示测试依赖旧执行代码，不再作为本版桌面的验收项目。

以下是 `260004` 当时的验证边界：该轮未安装新包到手机，堆叠切换、应用内与桌面进入、多任务点击和删除、横竖屏、清空后的原厂提示及清理按钮操作留待真机验收。后续 `260005` 的安装和用户验收状态见本文开头；用户整体反馈不等于逐项补齐历史测试记录。恢复已移除的增强功能仍需另行实现。
