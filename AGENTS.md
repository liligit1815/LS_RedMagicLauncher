# 桌面版本约定

- 显示版本 `versionName` 由用户决定，不擅自命名、添加后缀或从数字推断日期规则。用户未指定新显示版本时保留现有值。
- 用户指定的起始修改版为 `26.9.260.908.2609081608`，内部版本号 `260000`。
- 当前用户已验收版本为 `260005`；本轮横屏最近任务修复使用 `260006`，本轮修正、验证与重复构建不重复加号。下一轮独立功能修改使用 `260007`。每一轮实际功能修改将当前内部版本号 `versionCode` 加 `1`；只更新说明、迁移目录或整理发布流程不另起功能版本。
- 版本打包来源是 `src/apktool.yml` 的 `versionInfo`；检查最终 APK 和安装到手机后的版本字段，不能只改文件名。
- 版本变更后用 Apktool `b -f` 强制回编译，避免旧 Manifest 缓存保留旧版本。
- 更换桌面显示版本或原厂底包时，同步检查模块 `LauncherCompatibility`，避免已记录修改版因命名不同而被误报为底包不匹配。

# 独立工程边界

- 本目录是独立的红魔修改版桌面工程，使用本目录自己的 Git 仓库、源码、原厂底包、工具、框架缓存和本地签名配置；构建入口为 `tools/build-launcher.py`。
- 路径从本工程根目录解析，不读取父目录 LS_Augment 模块的源码、构建脚本或签名文件。Android SDK 和 JDK 使用本机安装位置。
- 上述模块兼容性检查属于两个产品的版本协作，不是桌面构建依赖；需要修改模块时，先确认任务范围，不在桌面任务中顺带修改父工程。
- `audit/`、`archive/` 和旧 `out/` 中的脚本、日志及绝对路径是历史证据。后续构建以当前 `README.md` 和 `tools/` 为准，不执行历史构建入口。
- 当前源码、固定回归基线与构建工具均随独立源码仓库保存；默认基线为 `baselines/launcher-260005.apk`，不得改回被忽略的历史 `out/`。固定框架输入 `framework/cache/1.apk` 不能当普通缓存删除。

# 公开源码与私有签名

- 源码仓库为 `git@github.com:liligit1815/LS_RedMagicLauncher.git`，默认分支 `main`；签名仓库为 `git@github.com:liligit1815/LS_SSHKey.git`，签名仓库必须保持私有。
- APK 签名材料在私有仓库的 `redmagic-launcher/` 下维护，使用 `python tools/restore-signing.py --from-repo <私有仓库克隆目录>` 核验并恢复至本工程 `.signing/`，构建不直接依赖私有仓库路径。
- 不生成新密钥冒充原有升级身份，不在源码、提交消息、公共日志或报告中写入私钥、密码、设备数据。公开证书与证书指纹是可公开的身份校验信息。
- 签名仓库只保存该项目已授权的 APK 签名材料，不混入电脑的 GitHub SSH 私钥、账户令牌或其他项目凭据。
- 提交源码前检查 `.signing/`、`out/`、`audit/`、`archive/`、`migration/` 和缓存仍被忽略；保留 `.gitattributes`，避免 Git 换行改变固定打包输入。
- 构建输入、当前功能与验收状态应在随 Git 保存的说明中可读，不能只指向旧电脑的绝对路径或被忽略的历史证据。当前进度见 `docs/DEVELOPMENT_STATUS.md`；安装前自动预检和恢复演练尚未完成，不得在说明中写为已支持。
